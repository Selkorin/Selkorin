<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="22dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/add_key_title"
        android:textColor="@color/text"
        android:textSize="17sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="16dp"
        android:text="@string/add_key_sub"
        android:textColor="@color/muted"
        android:textSize="12.5sp"
        android:lineSpacingMultiplier="1.3" />

    <EditText
        android:id="@+id/keyNameInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_input"
        android:hint="@string/key_name_hint"
        android:textColorHint="@color/dim"
        android:textColor="@color/text"
        android:textSize="13sp"
        android:maxLines="1" />

    <EditText
        android:id="@+id/keySecretInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:background="@drawable/bg_input"
        android:hint="@string/key_secret_hint"
        android:textColorHint="@color/dim"
        android:textColor="@color/text"
        android:textSize="12sp"
        android:fontFamily="monospace"
        android:gravity="top"
        android:minLines="4"
        android:maxLines="8"
        android:inputType="textMultiLine" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="18dp">

        <Button
            android:id="@+id/addKeyCancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_btn_ghost"
            android:text="@string/cancel"
            android:textColor="@color/text"
            android:textAllCaps="false"
            android:elevation="0dp"
            android:layout_marginEnd="10dp" />

        <Button
            android:id="@+id/addKeySave"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_btn_primary"
            android:text="@string/save"
            android:textColor="@color/bg"
            android:textStyle="bold"
            android:textAllCaps="false"
            android:elevation="0dp" />
    </LinearLayout>
</LinearLayout>
