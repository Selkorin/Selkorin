# Ядро (обязательно)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
python-multipart>=0.0.9

# Интеграции (нужны для live-режима; демо работает и без них)
telethon>=1.34        # Telegram-сессии, парсинг чатов, рассылки, реакции, кружки
anthropic>=0.40       # AI-скоринг лидов и генерация сообщений (Claude)
httpx>=0.27           # Яндекс.Карты / Яндекс-поиск (официальные API)
