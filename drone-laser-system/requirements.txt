# Ядро (обязательно)
numpy>=1.24
PyYAML>=6.0

# Компьютерное зрение (обязательно для работы с реальной камерой;
# детектор имеет numpy-fallback для запуска тестов без OpenCV)
opencv-python>=4.8 ; platform_machine != "unknown"

# Управление реальным приводом/лазером по последовательному порту
pyserial>=3.5

# Тесты
pytest>=7.4
