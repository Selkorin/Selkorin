"""Selkorin Sky Sentinel — ПО системы мониторинга неба и лазерного наведения.

Пакет разбит на слои:
  detection  — компьютерное зрение (поиск целей на кадре);
  tracking   — сопровождение целей (фильтр Калмана) и геометрия координат;
  control    — управление двухосевым поворотным механизмом (PID + HAL);
  laser      — управление лазером (через блокировки безопасности);
  safety     — менеджер безопасности и все интерлоки;
  core       — оркестратор и конечный автомат;
  sim        — симулятор для запуска без реального оборудования.
"""

__version__ = "0.1.0"
