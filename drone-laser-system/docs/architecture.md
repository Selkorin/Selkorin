# Архитектура системы

## 1. Общая структурная схема

```mermaid
flowchart LR
    subgraph SENSE["Сенсоры / оптика"]
        CAM["Камера (моно, высокая\nчувствительность)"]
        IR["ИК-подсветка 850/940 нм\nили тепловизор"]
        AMB["Датчик освещённости"]
        PIR["Датчик присутствия\nчеловека (интерлок)"]
    end

    subgraph COMPUTE["Вычислитель (Raspberry Pi 5 / Jetson)"]
        DET["Детектор целей\n(OpenCV)"]
        TRK["Трекер\n(фильтр Калмана)"]
        COORD["Пересчёт\nпиксель → угол"]
        ORCH["Оркестратор\n(конечный автомат)"]
        SAFE["Менеджер\nбезопасности"]
    end

    subgraph ACT["Исполнительные устройства"]
        MCU["Контроллер осей\n(Arduino/STM32)"]
        PAN["Привод азимута\n(шаговый + драйвер)"]
        TILT["Привод возвышения\n(шаговый + драйвер)"]
        LSR["Лазер (подсветка /\nуничтожение)"]
        ESTOP["E-STOP + ключ\n+ индикатор"]
    end

    CAM --> DET
    IR -.освещает сцену.-> CAM
    DET --> TRK --> COORD --> ORCH
    AMB --> SAFE
    PIR --> SAFE
    ORCH --> SAFE
    ORCH -->|углы| MCU
    SAFE -->|разрешение огня| MCU
    MCU --> PAN
    MCU --> TILT
    MCU --> LSR
    ESTOP -.аппаратный разрыв.-> LSR
    ESTOP --> MCU
```

## 2. Программные модули и потоки данных

```mermaid
flowchart TD
    A["camera.read() → frame"] --> B["detection.Detector.process(frame)\n→ list[Detection]"]
    B --> C["tracking.Tracker.update(detections)\n→ list[Track]"]
    C --> D["выбор активной цели\n(приоритет, уверенность)"]
    D --> E["coordinates.pixel_to_angles(track, gimbal)\n→ (pan, tilt)"]
    E --> F["control.Gimbal.aim(pan, tilt)\nPID → команды приводам"]
    F --> G{"наведены на цель?\n(ошибка < порога)"}
    G -->|нет| F
    G -->|да| H["safety.SafetyManager.evaluate(target, state)"]
    H -->|разрешено| I["laser.Laser.fire(mode)"]
    H -->|запрещено| J["laser.Laser.off()\n+ лог причины"]
```

## 3. Конечный автомат оркестратора

```mermaid
stateDiagram-v2
    [*] --> BOOT
    BOOT --> SELFTEST: инициализация
    SELFTEST --> SAFE: самопроверка OK
    SELFTEST --> FAULT: ошибка
    SAFE --> SEARCH: взведение ключом
    SEARCH --> TRACK: цель обнаружена
    TRACK --> SEARCH: цель потеряна
    TRACK --> AIM: цель устойчива
    AIM --> TRACK: цель ушла из сектора
    AIM --> ENGAGE: наведён + безопасность OK
    ENGAGE --> AIM: цель смещается
    ENGAGE --> SAFE: цель поражена/ушла
    SAFE --> FAULT: отказ
    FAULT --> SAFE: сброс оператором
    SEARCH --> SAFE: E-STOP
    TRACK --> SAFE: E-STOP
    AIM --> SAFE: E-STOP
    ENGAGE --> SAFE: E-STOP
```

## 4. Разделение ответственности (контроллер осей vs вычислитель)

| Функция | Где выполняется | Почему |
|---------|-----------------|--------|
| Компьютерное зрение, трекинг | Вычислитель (Python) | Требует вычислительной мощности |
| Пересчёт координат, стратегия | Вычислитель (Python) | Гибкость, логирование |
| Замкнутый контур приводов | Контроллер осей (C/прошивка) | Детерминизм, реальное время |
| Аппаратные блокировки, E-STOP | Контроллер осей + аппаратно | Безопасность не должна зависеть от ОС |
| Индикация излучения | Аппаратно | Требование норм безопасности |

Вычислитель шлёт контроллеру целевые углы и запросы огня по последовательному
порту (протокол — см. [electronics.md](electronics.md#протокол-обмена)); контроллер
отвечает телеметрией и **вправе отклонить** небезопасную команду.

## 5. Временной бюджет (цель — 30 кадров/с)

| Этап | Бюджет, мс |
|------|-----------|
| Захват кадра | 5 |
| Детектор (вычитание фона, контуры) | 10 |
| Трекер (Калман, ассоциация) | 2 |
| Пересчёт координат + стратегия | 1 |
| Отправка команд, телеметрия | 2 |
| Резерв | 13 |
| **Итого** | **≤ 33 мс** |
